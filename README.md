# 薬剤師体験 デジタルフォトフレーム v2

## 修正内容
- 起動時を背面カメラ優先に変更
- カメラ映像をフレーム中央の透明窓の中だけに表示
- フレーム外へカメラ映像がはみ出さないように修正
- 保存画像も画面と同じ位置で合成
- カメラ切替ボタンは残しています
- 「薬剤師認定！」＋紙吹雪0.5秒はそのまま
- レアフレームは6分の1

## GitHubでの差し替え
既存リポジトリの `index.html`、`style.css`、`app.js` をこのv2版で上書きしてください。
`frames`フォルダはそのままでも動きます。
